<!DOCTYPE html>
<html>
<head>
  <title>Edit User</title>
  <link rel="stylesheet" href="<%= BASE_PATH %>/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="header-title">
      <img src="<%= BASE_PATH %>/exercises/img/nhs.svg" alt="NHS Logo" class="header-logo">
      <h1 class="header-title-text">PhysioHUB</h1>
    </div>
    <hr>

    <h1>Edit User:</h1>
    <h2><%= escapeHTML(patient.name) %> <%= escapeHTML(patient.surname) %></h2>
    <% if (messages.success) { %><p class="success"><%= messages.success %></p><% } %>
    <% if (messages.error) { %><p class="error"><%= messages.error %></p><% } %>


    <form method="POST" action="<%= BASE_PATH %>/admin/edit/<%= patient.id %>">

      <input type="text" name="name" value="<%= escapeHTML(patient.name) %>" required>
      <input type="text" name="surname" value="<%= escapeHTML(patient.surname) %>" required>
      <input type="email" name="email" value="<%= escapeHTML(patient.email) %>" required>
      <textarea name="illness"><%= escapeHTML(patient.illness) %></textarea>
      <button type="submit">Update</button>
    </form>
    <br>
    <a href="<%= BASE_PATH %>/admin/dashboard">Back to Dashboard</a>

    <a href="<%= BASE_PATH %>/logout" style="margin-left: 20px;">Logout</a>
</body>
</html>